package matrix

import "strings"

func New(inputNumbers string) {
	rows := strings.Split(inputNumbers, "\n")

	rowInt := 0
	colInt := 0
	for index, row := range rows {
		rowValue := strings.Split(row, " ")

	}

}
